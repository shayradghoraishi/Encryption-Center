import React from "react";
import { EyeOff, Database, Network, Clipboard, Trash2 } from "lucide-react";
import PageHeader, { SecurityNote } from "@/components/PageHeader";
import SecurityBadge from "@/components/SecurityBadge";
import { useI18n } from "@/lib/i18n";

const rows = [[Network,"Network requests","The application does not need a server to encrypt or decrypt your data."],[Database,"Local storage","Preferences, history and the optional encrypted Key Vault are stored locally."],[Clipboard,"Clipboard","Copying sensitive output uses the browser clipboard API only when you request it."],[Trash2,"Data retention","Operation history can be cleared from Settings. The Key Vault can be locked or destroyed separately."]];
export default function Privacy(){const {t}=useI18n();return <div className="mx-auto max-w-4xl px-4 py-6 sm:px-8 sm:py-10"><PageHeader icon={EyeOff} title={t("privacy.title")} subtitle={t("privacy.subtitle")}><SecurityBadge level="secure"/></PageHeader><SecurityNote variant="info">{t("privacy.note")}</SecurityNote><div className="mt-6 space-y-3">{rows.map(([I,k,v])=><div key={k} className="flex gap-4 rounded-2xl border border-border p-5"><div className="rounded-xl bg-muted p-3"><I className="h-5 w-5 text-red-400"/></div><div><h3 className="font-semibold">{k}</h3><p className="mt-1 text-sm leading-6 text-muted-foreground">{v}</p></div></div>)}</div><div className="mt-6 rounded-2xl border border-border bg-card/40 p-5"><h3 className="font-semibold">Important limitation</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">A browser application cannot guarantee that every trace of sensitive data is immediately erased from process memory. Encryption Center therefore avoids making claims such as “military-grade memory wiping” or “impossible to trace.” Your device, browser, extensions, operating system and backups remain part of the security boundary.</p></div></div>}
